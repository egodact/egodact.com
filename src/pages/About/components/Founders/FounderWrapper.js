import styled from 'react-emotion';

const FounderWrapper = styled('section')({
  marginTop: 36,
  marginLeft: -24,
  marginRight: -24,
  display: 'flex',
  flexWrap: 'wrap'
});

export default FounderWrapper;
