import styled from 'react-emotion';

const FounderPicture = styled('img')({
  marginBottom: 12,
  width: 200,
  height: 200,
  backgroundColor: '#BDBDBD',
  border: '8px solid #E0E0E0',
  borderRadius: '50%',
  boxSizing: 'border-box'
});

export default FounderPicture;
