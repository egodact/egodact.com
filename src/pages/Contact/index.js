import React, { Fragment } from 'react';
import { Helmet } from 'react-helmet';

const Contact = () => (
  <Fragment>
    <Helmet>
      <title>Contact</title>
    </Helmet>
  </Fragment>
);

export default Contact;
