import styled from 'react-emotion';

const ArrowButtonLabel = styled('h4')({
  marginBottom: 0,
  maxWidth: 300,
  lineHeight: 1.5,
  fontSize: 24,
  fontWeight: 500
});

export default ArrowButtonLabel;
